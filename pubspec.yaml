name: always
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  firebase_core: ^4.2.1
  firebase_auth: ^6.1.2
  go_router: ^16.3.0
  provider: ^6.1.5+1
  cloud_firestore: ^6.1.0
  timeago: ^3.7.1
  firebase_database: ^12.0.3
  http: ^1.5.0
  intl: ^0.20.2
  google_fonts: ^6.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0
  flutter_launcher_icons: ^0.14.4

flutter_icons:
  android: true
  ios: false
  image_path: "images/memoa.png"

flutter:
  uses-material-design: true
  assets:
    - images/
